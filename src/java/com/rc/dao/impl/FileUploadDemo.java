package com.rc.dao.impl;

import java.io.FileInputStream;
import java.io.IOException;

//import org.apache.commons.net.ftp.FTPClient;

public class FileUploadDemo {
	
	public static void main(String arg[]){/*
		
		//FTPClient client = new FTPClient();
		FileInputStream fis = null;

		try {
		   // client.connect("bailiwicksolution.com");
		    //boolean login = client.login("managbyc", "B@$#%*90!@i");
		   // System.out.println("login "+ login);

		    //
		    // Create an InputStream of the file to be uploaded
		    //
		    String filename = "C:\\Users\\Admin\\Pictures\\Feedback\\a\\Capture001.png";
		    fis = new FileInputStream(filename);

		    //
		    // Store file to server
		    //
		    boolean storeFile = client.storeFile(filename, fis);
		    System.out.println("store file "+storeFile);
		    client.logout();
		} catch (IOException e) {
		    e.printStackTrace();
		} finally {
		    try {
		        if (fis != null) {
		            fis.close();
		        }
		        client.disconnect();
		    } catch (IOException e) {
		        e.printStackTrace();
		    }
		}
		
		
	*/}

}
